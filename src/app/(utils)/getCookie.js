// "use client";
// function getCookie(name) {
//   // Create a RegExp to match the cookie name
//   const cookiePattern = new RegExp(`(^| )${name}=([^;]*)(;|$)`);
//   const match = document.cookie.match(cookiePattern);

//   // If a match is found, return the cookie value; otherwise, return null
//   return match ? decodeURIComponent(match[2]) : null;
// }

// // Sử dụng hàm để lấy cookie
// export const loggedUser = getCookie("name");
